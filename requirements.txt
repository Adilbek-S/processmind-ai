# Нижние границы — версии, на которых проект реально проверен (Python 3.14.5, чистая установка + полный набор тестов).
# Более старые версии не проверялись: код использует API новых версий (langgraph interrupt/Command, openai chat.completions.parse, MCP SDK 2.x).
streamlit>=1.64
langgraph>=1.2
openai>=3.0
chromadb>=1.5
pymupdf>=1.28
pillow>=12.0
graphviz>=0.21
langsmith>=0.14
pydantic>=2.13
pydantic-settings>=2.15
python-dotenv>=1.0
mcp>=2.2
pytest>=9.0
