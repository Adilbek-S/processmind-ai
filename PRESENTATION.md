# PRESENTATION — структура презентации (13 слайдов)

Готовая презентация: [docs/ProcessMind_AI_presentation.pdf](docs/ProcessMind_AI_presentation.pdf) (исходник — `docs/presentation/slides.html`; пересборка: Chrome `--headless --print-to-pdf`). Таблица ниже — план, по которому она собрана.

Расчёт: 10 минут защиты (см. [DEMO.md](DEMO.md)). Числа — из [EVALS.md](EVALS.md); при обновлении оценок сверить заново.

| № | Слайд | Содержание | Визуал | ≈ время |
|---|---|---|---|---|
| 1 | **Титул** | ProcessMind AI — анализ и автоматизация бизнес-процессов; автор; курс LLM Engineering; пометка «синтетические данные» | название + подзаголовок | 0:15 |
| 2 | **Проблема** | Ручной анализ: долго, неповторяемо, выдуманные допущения об эффекте | «до»: документ → таблица в Excel → «на глаз» | 0:45 |
| 3 | **Решение** | Документ → ProcessSpec → метрики → рекомендации с источниками → модельная оценка TO-BE → отчёт. Принципы: не выдумывать, LLM предлагает / код проверяет / человек подтверждает | конвейер в 6 блоков | 1:00 |
| 4 | **Демо: извлечение** | PDF → таблица операций, пустые значения остаются пустыми, правка, схема AS-IS | скриншот/живое демо | 1:15 |
| 5 | **Демо: анализ и решение** | метрики, проблемы, паттерны, рекомендации; пауза `interrupt`; TO-BE, сравнение, отчёт | скриншот/живое демо | 2:00 |
| 6 | **Vision** | PNG → процесс; честно: связи и тип операции могут читаться неверно, система предупреждает | схема-вход и результат рядом | 0:45 |
| 7 | **Архитектура** | схема из ARCHITECTURE.md: Streamlit, parsing, LangGraph, RAG, MCP, Skill, LangSmith | mermaid-диаграмма | 1:00 |
| 8 | **LangGraph и human-in-the-loop** | 8 узлов, условные переходы к отчёту при ошибках, пауза на подтверждении, программная валидация рекомендаций | граф с выделенной паузой | 1:00 |
| 9 | **RAG** | 14 паттернов, chunking по разделам, text-embedding-3-small, ChromaDB, Top-3; Hit@3 = 19/23 (83%); примеры промахов | таблица метрик | 0:45 |
| 10 | **MCP и Skill** | 3 инструмента (validate, metrics, simulate) — отдельный процесс, расчёты без LLM; SKILL.md — методика, читается при каждом запуске | список инструментов, фрагмент SKILL.md | 0:45 |
| 11 | **Оценки и A/B** | Golden Dataset 30; F1 по смыслу: A 0.696 / B 0.725 (нет различия: в другом прогоне −0.014); ссылки на паттерны 0% → 100%; отрицательные 4/4 → 1/4 (n=4); +2007 токенов, +2.97 с | таблица A/B | 1:15 |
| 12 | **Гиперпараметры и ограничения** | temperature 0.0 по эксперименту (правило задано заранее); top_p, max_tokens; модель не сравнивалась; LangSmith-облако не проверено; синтетические данные; `MemorySaver` | таблица «выбрано / основание» + список ограничений | 0:45 |
| 13 | **Выводы и дальше** | что сделано; что не подтверждено; развитие: реальные процессы, сравнение моделей, персистентный checkpointer, улучшение чтения связей | 3–4 тезиса | 0:30 |

Резерв (не показывать без вопроса): таблица результатов по примерам (приложение EVALS.md), эксперимент с temperature (числа), состав Skill.

Правила оформления: один тезис на слайд; каждое число с источником (EVALS.md); слово «прогноз» не использовать — только «модельная оценка».
